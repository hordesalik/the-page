<div class="p-3">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            {{#breadcrumbs}}
                <li class="breadcrumb-item {{#active}}active{{/active}}">
                    {{#url}}
                        <a href="{{url}}">{{label}}</a>
                    {{/url}}

                    {{^url}}
                        {{label}}
                    {{/url}}
                </li>
            {{/breadcrumbs}}
        </ol>
    </nav>
</div>
